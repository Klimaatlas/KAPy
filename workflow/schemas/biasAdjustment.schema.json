{
  "title": "KAPy bias adjustment configuration",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Bias adjustment is the process by which the outputs of a climate model are post-processed so that they match specific characteristics of the observed climate during a common period of time. In KAPy we wrap methods in the `xsdba` package, previously part of `xclim. The method employed is chosen via the `method` field, with the following valid options: \n * `xsdba-scaling` - [Scaling bias-adjustment](https://xsdba.readthedocs.io/en/stable/apidoc/xsdba.html#xsdba.adjustment.Scaling).\n * `xsdba-eqm` - [Empirical Quantile Mapping](https://xsdba.readthedocs.io/en/stable/apidoc/xsdba.html#xsdba.adjustment.EmpiricalQuantileMapping). \n * `xsdba-dqm` - [Detrended Quantile Mapping](https://xsdba.readthedocs.io/en/stable/apidoc/xsdba.html#xsdba.adjustment.DetrendedQuantileMapping). \n  \n In addition, the ability to add custom indicator functions may be added in a future verison. \n   For more information, see the documentation in the `xsdba` package: https://xsdba.readthedocs.io/en/stable/index.html",
  "type": "object",
  "required": [
    "id",
    "enabled",
    "outDatasetCode",
    "baVariable",
    "targetDataset",
    "refDataset",
    "outputGrid",
    "trainPeriodStart",
    "trainPeriodEnd",
    "method",
    "additional_arguments"
  ],
  "properties": {
    "id": {
      "type": "string",
      "uniqueItems": true,
      "pattern": "^[^ ,]+$",
      "description": "Unique identifier for the bias adjustment task. Cannot contain a space or comma."
    },
    "enabled": {
      "description": "Determines whether the row be used in the workflow. A non-empty value indicates not to use that row.",
      "type": ["string","null"]
    },
    "outDatasetCode": {
      "type": "string",
      "description": "DatasetCode that will be associated with bias-adjusted output. Note that the cominbation of variable name and DatasetCode must not be a duplicate of other combinations names found in the configuration of KAPy. This variable primarily serves to distinguish between raw and bias-adjusted variables e.g. `CORDEX` and `CORDEX-cal` The exact choice is not important, but it is recommended to adopt a consistent approach throughout the project."
    },
    "baVariable": {
      "type": "string",
      "description": "The name of the variable to bias adjust, drawn from the list of primary and secondary variables. If the variable cannot be found, an error will be raised. KAPy assumes that the same variable name is used for both the model and observational data sources."
    },
    "targetDataset": {
      "type": "string",
      "description": "The dataset that will be bias adjusted e.g.  'CORDEX'. This is most commonly the output of a climate model, but need not be."
    },
    "refDataset": {
      "type": "string",
      "description": "The dataset that will be used as the reference  data source to bias-adjust against, such as `ERA5`. Most commonly this will be an observational data set, a reanalysis or similar but it need not be. However, the combination of climate variable and dataset code should uniquely identify a single file within the variable palette of KAPy - if not, an error will be raised."
    },
    "outputGrid": {
      "type": "string",
      "description": "Chooses the grid that the output of bias adjustment procedure ends on. Arguments can be made that the target dataset should be adjusted to the grid of the reference data (`reference`), or conversely, that the reference dataset should be adjusted onto the grid of the target dataset (`target`). In cases where there is a substantial difference in resolutions between the grids, this decision can potentially lead to the loss by omitting points from the higher resolution grid: however, this may also be desirable in some cases. Given that the appropriate choice will depend on the specifics of the system, this choice is therefore left to the user, as configured by this option. ",
      "enum": [
        "reference",
        "target"
    ]
    },
    "trainPeriodStart": {
      "type": "string",
      "pattern": "^\\d{4}$",
      "description": "Start year of the training period. Data after and including 1 Jan of this year will be used for training the bias adjustment procedure."
    },
    "trainPeriodEnd": {
      "type": "string",
      "pattern": "^\\d{4}$",
      "description": "End year of the training period. Data before and including 31 Dec of this year will be used for training the bias adjustment procedure."
    },
    "method": {
      "type": "string",
      "description": "Bias adjustment method to be used. See above for a clarification of options.",
      "enum": [
        "xsdba-scaling",
        "xsdba-eqm",
        "xsdba-dqm"
    ]
    },
    "grouping": {
      "type": "string",
      "description": "Apply bias-adjustment independently of each time grouping selected here.",
      "enum": [
        "none",
        "dayofyear",
        "month",
        "season"]
    },
    "additional_arguments": {
      "type": "string",
      "nullable": true,
      "description": "Additional arbitrary arguments specified as a dict to be passed to the function via keyword arguments. e.g. `{'kind'='+', group='time.month'}`. Can be an empty dict or empty string if no there are no additional parameters. e.g. `{}` "
    },
    "customScriptPath": {
      "type": "string",
      "nullable": true,
      "description": "If `method` is set to `custom`, this field is used to identify the path to a custom script, if applicable."
    },
    "customScriptFunction": {
      "type": "string",
      "nullable": true,
      "description": "Name of the function in `customScriptPath` to be used for bias correction,  if applicable."
    }
  },
  "additionalProperties": false
}

